---

- step:
    name: batch inference
    image: busybox
    command:
     - ls -laR /valohai/inputs
    environment-variables:
      - name: foo
        default: bar
      - name: baz
        default: "850"
        optional: false

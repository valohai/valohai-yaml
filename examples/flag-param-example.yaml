- step:
    name: test
    image: busybox
    command: foo {parameters}
    parameters:
      - name: case-insensitive
        type: flag
        description: whether to be case insensitive
      - name: defaults-to-true
        type: flag
        default: true
        description: I will be true by default
